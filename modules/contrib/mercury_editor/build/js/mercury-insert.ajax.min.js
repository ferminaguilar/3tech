(function(){"use strict";((Drupal,drupalSettings)=>{const parseFragment=htmlString=>{const template=document.createElement("template");return template.innerHTML=htmlString,template.content},getInsertedElements=fragment=>Array.from(fragment.childNodes).filter(node=>node.nodeType===Node.ELEMENT_NODE),insertionHandlers={before(target,fragment){target.parentNode.insertBefore(fragment,target)},after(target,fragment){target.parentNode.insertBefore(fragment,target.nextSibling)},append(target,fragment){target.appendChild(fragment)},prepend(target,fragment){target.insertBefore(fragment,target.firstChild)}},runInsert=(response,operation,settings)=>{if(!response.selector||!response.data)return;const insertHandler=insertionHandlers[operation];if(!insertHandler)return;const matches=document.querySelectorAll(response.selector);if(!matches.length)return;const target="after"===operation||"append"===operation?matches[matches.length-1]:matches[0];if(!target.parentNode)return;const fragment=parseFragment(response.data),insertedElements=getInsertedElements(fragment);insertHandler(target,fragment),insertedElements.forEach(element=>{document.documentElement.contains(element)&&Drupal.attachBehaviors(element,settings)})};Drupal.AjaxCommands.prototype.mercuryBefore=function(ajax,response){const settings=response.settings||ajax.settings||drupalSettings;runInsert(response,"before",settings)},Drupal.AjaxCommands.prototype.mercuryAfter=function(ajax,response){const settings=response.settings||ajax.settings||drupalSettings;runInsert(response,"after",settings)},Drupal.AjaxCommands.prototype.mercuryAppend=function(ajax,response){const settings=response.settings||ajax.settings||drupalSettings;runInsert(response,"append",settings)},Drupal.AjaxCommands.prototype.mercuryPrepend=function(ajax,response){const settings=response.settings||ajax.settings||drupalSettings;runInsert(response,"prepend",settings)}})(Drupal,drupalSettings)})();
