<section id="services" class="government-services-section">
  <div class="grid-container">
    <div class="grid-row grid-gap">
      {% for key, item in content.field_services %}
        {% if key|first != '#' %}
          {{ item }}
        {% endif %}
      {% endfor %}
    </div>
  </div>
</section>