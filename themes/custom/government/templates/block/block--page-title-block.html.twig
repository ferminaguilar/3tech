{#
/**
 * @file
 * Theme override for page title block in Government theme.
 *
 * Left intentionally empty because the main page H1 title is rendered
 * in the static hero banner (.government-page-header) at the top of the page.
 */
#}
